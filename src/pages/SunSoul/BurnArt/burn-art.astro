---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

import { wallpapers } from '/src/data/wallpaper.ts';
import { grove } from '/src/data/wicked.ts';
import { wickedBurnArt } from '/src/data/wicked.ts';
import { wickedBurns } from '/src/data/wicked.ts';

import './src/styles/global.css';
import './src/styles/burn-art-page.css';

const wallpaper = wallpapers.garden[0];

const tameBurns = wickedBurns.tame;
const tarotBurns = wickedBurns.tarot;
const spicyBurns = wickedBurns.spicy;

const formatter = new Intl.ListFormat('en', { style: 'long', type: 'conjunction' , });

{console.log(spicyBurns)}
---

<body>
  <div id = "background"; class= "page-background" style={`background-image: url(${wallpaper.image.src})`}></div>

  <main>
    <section id="burnart" class="burn-art-header">
          <h1>Burn Art</h1>
    </section>

    <section class="burn-art-info">
      <h1>Burn</h1>

      <p> Tarot, Cute, Funny, Wholesome, Spicy </p>
      
      <p> Here we talk about the thing that the page represents and the arts and crafts involved with 
      it on a hand loved personal level. And all it takes it writing it out, all of it, eventually, 
      when the words themselves look pretty enough on the page and items fall from the sky when those 
      landmine links go "off" </p>
    </section>

    {console.log(spicyBurns.Tray.PO01)}
  </main>
</body>