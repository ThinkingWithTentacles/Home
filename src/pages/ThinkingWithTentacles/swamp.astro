---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

import { wallpapers } from '/src/data/resource.ts';
import { swamp } from '/src/data/swamp.ts';

import '/src/styles/global.css';
import '/src/styles/swamp-page.css';

import { 
  swampGlass, 
  swampLeather, 
  swampParacord, 
  swampWood, 
  swampWorkshop, 
} from '../../config.js';

const wallpaper = wallpapers.creature[1];
---

<body>
  <div
    id="background"
    class="swamp-page-background"
    style={`background-image: url(${wallpaper.image.src})`}>

    <a class="wood-wrapper" href={swampWood}>
        <span></span>
    </a>

    <a class="leather-wrapper" href={swampLeather}>
        <span></span>
    </a>

    <a class="shop-wrapper" href={swampWorkshop}>
        <span></span>
    </a> 
  </div>

  <main>
  </main>
</body>