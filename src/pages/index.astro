---
import Welcome from '../components/Welcome.astro';
import Layout from '../layouts/Layout.astro';

import { 
  swamp, 
  flower, 
  end,
  vault
} from '../config.js';
---

<Layout>
	<h1>Hello World</h1>
</Layout>

<main>
  <a href={swamp + "/landing"}>|   Thinking With Tentacles   |</a>;
  <a href={flower + "/landing"}>|   WickedGabi   |</a>;
  <a href={end + "/landing"}>|   Dead End   |</a>;
  <a href={vault + "/landing"}>|   Vault   |</a>;
</main>