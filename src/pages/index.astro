---
import Welcome from '../components/Welcome.astro';
import Layout from '../layouts/Layout.astro';

import { 
  swampLand, 
  wickedLand, 
  deadEnd,
  theVault
} from '../config.js';
---
<Layout>
	<h1>Hello World</h1>
</Layout>

<main>
  <a href={swampLand}>|   Thinking With Tentacles   |</a>;
  <a href={wickedLand}>|   WickedGabi   |</a>;
  <a href={deadEnd}>|   Dead End   |</a>;
  <a href={theVault}>|   Vault   |</a>;
</main>